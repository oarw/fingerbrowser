---
description: 完成代码任务后自动推送并完成 GitHub Actions 交付闭环
alwaysApply: true
---

# 自动交付流程

- 完成代码修改和本地静态检查后，不等待用户再次提醒，继续完成 Git 远程交付。
- 只暂存并提交当前任务相关文件；不得顺带提交无关改动、未跟踪文件或本地生成产物。
- 从最新目标分支创建任务分支，提交并推送，创建或更新 PR。
- 触发并持续跟踪对应的 GitHub Actions；对明确可修复的失败继续修改、推送并重新验证。
- CI 全部通过后合并 PR，除非用户明确要求只推送分支或暂不合并。
- `package.json` 为预发行版本时，`main` 构建成功后必须继续跟踪自动 Pre-release，确认版本标签、Windows 安装包和校验文件均已发布；稳定版本按标签发布工作流跟踪。
- 私有仓库 Actions 额度不可用时，可按本项目约定临时改为公开；操作前记录原可见性，无论成功或失败，工作流结束后都恢复为原可见性。
- 除仓库自带的自动发布工作流外，不手动创建版本标签或发布 Release，除非任务明确包含发版。
