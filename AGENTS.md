# 项目 Agent 约定

- 始终使用简体中文回复。
- 内置搜索端点返回 `404` 或明确不可用时，不要反复重试；立即改用当前可用的 MCP 搜索、网页抓取或浏览器工具，并继续核验权威一手来源。

## 验证与构建

- 本项目的单测、Electron 冒烟验证、构建、打包和发布统一在 GitHub Actions 中执行。
- 禁止在本地执行 `npm install`、`npm test`、`npm run build`、`npm run dist` 或其他会下载依赖、生成构建产物、打包发布的命令。
- 本地只进行源码阅读、静态检查和必要的编辑；需要验证时查看或触发对应的 GitHub Actions 工作流。

## 自动交付

- 完成代码修改和本地静态检查后，自动提交任务相关文件、推送任务分支、创建或更新 PR，并持续跟踪 GitHub Actions；不要等待用户再次提醒。
- 不得顺带提交无关改动、未跟踪文件或本地生成产物。对明确可修复的 CI 失败继续修改、推送并重新验证。
- CI 全部通过后自动合并 PR，除非用户明确要求只推送分支或暂不合并。
- `main` 分支构建成功后由工作流自动递增 `v<版本>-pre.N`、创建 Pre-release 并上传 Windows 安装包；必须跟踪到自动发布完成。除该工作流外，不要手动创建版本标签或发布 Release，除非任务明确包含发版。
- 私有仓库 Actions 额度不可用时，可临时改为公开运行工作流；操作前记录原可见性，无论成功或失败，工作流结束后都必须恢复为原可见性。
