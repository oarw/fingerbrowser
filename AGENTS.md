# 项目 Agent 约定

- 始终使用简体中文回复。
- 内置搜索端点返回 `404` 或明确不可用时，不要反复重试；立即改用当前可用的 MCP 搜索、网页抓取或浏览器工具，并继续核验权威一手来源。

## 验证与构建

- 本项目的单测、Electron 冒烟验证、构建、打包和发布统一在 GitHub Actions 中执行。
- 禁止在本地执行 `npm install`、`npm test`、`npm run build`、`npm run dist` 或其他会下载依赖、生成构建产物、打包发布的命令。
- 本地只进行源码阅读、静态检查和必要的编辑；需要验证时查看或触发对应的 GitHub Actions 工作流。
