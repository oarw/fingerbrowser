# FingerBrowser 进度与计划

最后更新:2026-08-13

## 一、总体目标

做一个**自用、无广告、无功能限制**的指纹浏览器管理器,替代 RoxyBrowser。核心能力:多环境隔离、真实指纹伪装(内核级)、代理管理、批量运营、后续自动化。

路线选型(已定):**复用开源内核级指纹 Chromium(fingerprint-chromium)+ 自研 Electron 管理器**。不自己改 Chromium C++ 源码。

## 二、进度总览

| 阶段 | 状态 | 说明 |
|---|---|---|
| 阶段一 MVP | ✅ 已完成 | 环境管理 + 隔离 + 指纹参数 + 一键启动 |
| 阶段二 代理与批量 | ✅ 已完成 | 认证代理桥接 + IP 自动填充 + 批量平铺 |
| 阶段三 自动化与生态 | ⏳ 未开始 | 本地 API / CDP-RPA / 代理订阅 / 备份 |
| 阶段四 打磨与分发 | ⏳ 未开始 | 图标、自动更新、跨平台、测试 |

CI 状态:Windows 构建 + electron-builder 打包在 GitHub Actions 上稳定通过(约 2.5 分钟)。

## 三、已完成明细

### 阶段一 MVP(已交付)
- Electron + Vue 3 + electron-vite 骨架,主/预加载/渲染三进程与 IPC 打通。
- 环境增删改查,分组、标签、搜索。
- 每环境独立 `--user-data-dir` 物理隔离。
- 指纹参数可视化编辑:种子、系统及版本、品牌及版本、CPU 核心数、时区、语言、接受语言、WebRTC 策略;一键"随机种子/全部随机"生成自洽指纹。
- 无认证代理(HTTP/SOCKS5)配置。
- 一键启动/关闭,实时运行状态(圆点指示 + 运行态订阅)。
- 设置:指定内核路径(带文件选择框)、默认起始页。
- 本地 JSON 持久化。

### 阶段二 代理与批量(已交付)
- **认证代理 / SOCKS5 本地桥接**:`proxy-chain` 在 127.0.0.1 起匿名代理携带凭据转发,浏览器只连本地端口;随进程生命周期自动起停。
- **按代理 IP 自动填充**:经代理请求 IP 地理接口,自动带出时区/语言/接受语言,保证网络出口与指纹地区自洽。
- **批量启动**:卡片多选,支持"批量启动"。
- **窗口平铺**:按屏幕工作区算网格,给每个窗口传 `--window-position`/`--window-size`,错峰 400ms 启动。
- 依赖外部化(`externalizeDepsPlugin`),`proxy-chain`/`undici` 正确打进产物(CI 验证通过)。

## 四、阶段三:自动化与生态(下一步)

按建议优先级排列,每项可独立交付。

### 3.1 本地 API 服务(建议优先)
- 在主进程起一个本地 HTTP 服务(默认 `127.0.0.1:50325`,仅绑本地)。
- 接口:列出环境、启动/关闭环境、返回该环境的 CDP 调试端口(`webSocketDebuggerUrl`)。
- 价值:让外部脚本(Puppeteer/Playwright)能 attach 到指定环境做自动化。
- 关键点:启动内核时加 `--remote-debugging-port=<每环境唯一端口>`,并从 `/json/version` 读取 ws 地址。

### 3.2 CDP 自动化 / 轻量 RPA
- 内置流程编排:打开网址、等待、点击、输入、截图、滚动。
- 通过 3.1 暴露的 CDP 端口驱动;流程可保存、按环境批量执行。
- 可选:录制回放。

### 3.3 代理订阅管理
- 集成 Mihomo(Clash Meta)或 Xray-core 内核下载与管理。
- 支持导入订阅链接 / 本地 YAML,节点延迟测速,环境绑定指定节点。
- 参考:Mirage-Browser(Mihomo)、GeekEZBrowser(Xray REALITY/VLESS)。

### 3.4 环境备份与同步
- 本地导出/导入环境配置(不含敏感 Cookie 或可选加密导出)。
- 可选 WebDAV / GitHub 备份。

## 五、阶段四:打磨与分发

- 应用图标、品牌视觉、NSIS 安装界面美化。
- 自动更新(electron-updater + GitHub Releases)。
- 正式启用 macOS 构建(CI 加 `macos-latest` job)与签名/公证评估。
- Linux AppImage 验证。
- 自动化测试:主进程单测(参数拼接、桥接判定、网格计算)、渲染层组件测试。
- 修复 CI 的 Node20 action 弃用告警(升级到新版 action)。

## 六、待办小项(Backlog)

- 地理位置经纬度注入(通过 CDP `Emulation.setGeolocationOverride`),配合 3.1/3.2。
- 删除环境时可选"同时删除数据目录"。
- 环境复制(克隆)功能。
- 指纹一致性自检:内置打开 CreepJS/BrowserScan 并回读评分。
- 内核多版本管理:允许每环境指定不同内核版本路径。
- 启动参数自定义:高级用户可追加自定义 flags。
- 运行日志与错误提示优化(目前部分错误仅 alert)。

## 七、风险与注意

- **内核 GPU 指纹在 Windows 受限**:必要时评估切换/补充内核,或用 `--disable-spoofing=gpu`。
- **反检测是持续对抗**:内核需跟随上游更新;强依赖干净独立代理 IP,单靠指纹不够。
- **合规**:仅用于合法用途(隐私、测试、授权自动化),遵守目标站点条款。
- **私有仓库额度**:继续走"公开跑 CI、跑完私有";文档改动已通过 `paths-ignore` 免触发。

## 八、里程碑建议

1. M1(下一步):本地 API + CDP attach(打通自动化基础)。
2. M2:轻量 RPA 流程。
3. M3:代理订阅集成。
4. M4:图标 + 自动更新 + 首个正式 Release。
